<!-- Thanks for contributing to setup-jemalloc! -->

## Summary

<!-- What does this PR change, and why? Link the issue it closes, e.g. "Fixes #123". -->

## Changes

<!-- Bullet the notable changes. -->
-

## Testing

<!-- How was this verified? Tick what applies. -->
- [ ] `shellcheck` clean on changed shell scripts
- [ ] `bash tests/relocate_test.sh` and `bash tests/checksum_test.sh` pass
- [ ] CI green (`linux`, `linux_double_invocation`, `unit_tests`)
- [ ] Exercised the cache-miss path if the change touches download/build/install

## Notes

<!-- Anything reviewers should know: scope, follow-ups, platform caveats.
     Note: only Linux is supported today; macOS/Windows are unsupported. -->
